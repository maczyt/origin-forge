# origin-forge

```sh
Usage:
  $ origin-forge <command> [options]

Commands:
  new <libraryName>  create a library.

For more info, run any command with the `--help` flag:
  $ origin-forge new --help

Options:
  -h, --help     Display this message
  -v, --version  Display version number
```
